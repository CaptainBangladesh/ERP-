# 05 — Reporting dashboard: what it looks like

Type: prototype
Blocked by: 02, 03

## Question

Once phase-1 metrics (02) and the scaffolded module (03) exist, what does the reporting screen
actually look like — layout, which chart type per metric, drill-down from a summary number into
its detail (e.g. a valuation total down to the movements behind it). Use the `dataviz` skill for
chart/metric-visualization choices once this ticket is live.
