# 06 — Products and units of measure (Core), and the module generator

**What to build:** The product master as its own Core module beneath inventory — and the generator
that produces every module after it.

I can define units of measure and the products I deal in, with a cost, and edit or deactivate them.
Nothing is seeded, including units of measure — I create the ones my business uses. Products sits
beneath inventory so that Sales, Purchase, Manufacturing and E-commerce depend on Products rather
than on the whole inventory module.

The generator is extracted here, from three modules that already exist, and Products is the first
module produced by it. That ordering matters: a generator written before any module exists encodes
guesses, while one extracted from working modules encodes what actually turned out to be needed.
With forty-plus modules ahead, consistency cannot come from discipline — it comes from the starting
point being correct.

**Blocked by:** 05 — Parties (Core), and boundary enforcement

**Status:** ready-for-agent

- [ ] A command generates a new module given a name and a tier
- [ ] The generated module has a valid manifest, an explicit public contract, registered routes and
      navigation, and a migration set up
- [ ] The generated module passes the conformance pack and boundary enforcement immediately
- [ ] Generated frontend structure mirrors the backend module
- [ ] Generating a duplicate name, or a Core module depending on a higher tier, is refused
- [ ] A test proves a freshly generated module builds, boots and passes the pack
- [ ] Using the generator is documented as the only supported way to add a module
- [ ] Products is generated by the generator and is a Core module
- [ ] I can define units of measure by hand; none are seeded
- [ ] Units of measure can be grouped so related units convert between each other
- [ ] I can create a product with a code, a name and a unit of measure
- [ ] Product code is unique within my company and does not clash across companies
- [ ] I can record a cost against a product using the shared money primitive
- [ ] I can edit a product, and deactivate and reactivate it; products are never deleted
- [ ] A product can be marked as stockable or not, so non-stock items are supported
- [ ] I can link a product to its supplier parties through the Parties contract
- [ ] The public contract lets other modules read product details without touching its tables
- [ ] Backend tests cover creation, uniqueness, units, cost, deactivation and isolation
- [ ] Frontend tests cover the list, the forms, validation and the empty state
